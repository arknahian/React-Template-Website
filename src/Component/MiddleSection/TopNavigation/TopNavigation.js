import React from 'react';

const TopNavigation = () => {
    return (
        <nav>
            <input type='text'/>
            <span>123 ETH</span>
        </nav>
    );
};

export default TopNavigation;